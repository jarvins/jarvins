server:
  port: 9000
  servlet:
    context-path: '/${spring.application.name}'
spring:
  application:
    name: data-creator
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:mysql://ineffable.ink:33060/core?characterEncoding=UTF-8&allowPublicKeyRetrieval=true&useSSL=false
    hikari:
      username: P1cGFkH2YE
      password: r85yQGiz6i
      connection-test-query: select 1
      connection-timeout: 10000
      maximum-pool-size: 15
